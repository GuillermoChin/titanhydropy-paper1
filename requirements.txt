# Dependencias del snapshot, ancladas al entorno de la corrida de
# referencia. NO es un `pip freeze` del entorno entero: solo lo que el
# codigo importa de verdad.
#
# Python de referencia: 3.13.3 (requiere >= 3.10).
# Generado por tools/make_paper_package.py.
#
# SIN FECHA DE GENERACION, a proposito. Un sello de construccion hace que
# reconstruir el snapshot en otro dia produzca bytes distintos y, por tanto,
# otro SHA256SUMS. Quien reconstruya y compare checksums no podria
# distinguir un sello temporal de un cambio real de contenido: es el mismo
# defecto que el /CreationDate de los PDF. La fecha del release se declara
# UNA vez, en CITATION.cff.
#
# Nada de lo listado es especifico de un sistema operativo: se instala
# igual en Linux, macOS y Windows.

# OBLIGATORIA. Es el unico requisito para CALCULAR.
numpy==2.2.6

# OPCIONAL. Solo para las figuras; el runner avisa y sigue sin ella.
matplotlib==3.10.3

# Solo para ejecutar la suite y el test de reproduccion.
pytest==9.1.1

# --------------------------------------------------------------------
# Transitivas ancladas porque DETERMINAN LOS BYTES de las figuras.
#
# matplotlib escribe los PNG a traves de Pillow, subsetea las fuentes del
# PDF con fonttools, resuelve el layout con kiwisolver y analiza el
# mathtext con pyparsing. Una version distinta de cualquiera de ellas
# puede producir la MISMA imagen con OTROS bytes, y entonces
# `sha256sum -c` falla sin que se vea ninguna diferencia.
# --------------------------------------------------------------------

contourpy==1.3.2
cycler==0.12.1
fonttools==4.58.1
kiwisolver==1.4.8
packaging==25.0
pillow==12.3.0
pyparsing==3.2.3
python-dateutil==2.9.0.post0
six==1.17.0
